Simulator (bug fixes):
- 

Simulator (new features):
- Add deviations (map changes each time step)
- Change color of agent when it reaches goal
- Add support for wait operation (agents may wait at a position before
  continuing)


Map generator (new features):
- Have default arguments (i.e. if all arguments are not specified, run anyway)
- Add option to specify start/goal positions of agents
